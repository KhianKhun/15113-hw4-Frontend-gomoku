# Gomoku Frontend (GitHub Pages)

This is a simple HTML/CSS/JS Gomoku UI that calls a Flask backend hosted on Render.

## Setup
1) Deploy backend to Render and copy the public URL.
2) In `script.js`, set:
   - `BACKEND_URL = "https://your-backend.onrender.com"`

3) Push to GitHub and enable GitHub Pages.
